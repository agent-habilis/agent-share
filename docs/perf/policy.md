# agent-share performance baseline

Generated by `cargo task bench --tag policy`. Do not hand-edit — rerun it.

## Provenance

- commit: `e44acafc` (dirty working tree — numbers are not reproducible from this SHA)
- machine: Mac17,2 (10 cores)
- os: macOS 27.0
- rustc: rustc 1.95.0 (59807616e 2026-04-14)
- chrome: 149.0.7827.54
- wasm: 7381312 bytes
- corpus: 256 MiB · bench window: 15 s · repeats: 3

## Results

| cell | transport | direction | depth | RTT (ms) | MiB/s | connect (ms) | cores busy | user/sys (s) | spread |
|---|---|---|---:|---:|---:|---:|---:|---:|---:|
| `native-mount-cp` | quic | native->native (NFS) | 1 | — | 231.76 | — | 3.37 | — / — | 3% |
| `native-mount-cp-webrtc` | webrtc | native->native (NFS) | 1 | — | 4.23 | — | 2.22 | — / — | 23% |

`cores busy` is CPU-seconds per wall-second across producer and consumer: above 1.0 the cell is compute-bound, not waiting on the network. `user/sys` is the consumer's split — ciphers and copies land in `user`, syscalls and context switches in `sys`.

Mesh state per row:

- `native-mount-cp`: share mesh joined — shares the data endpoint and congestion domain
- `native-mount-cp-webrtc`: share mesh joined — shares the data endpoint and congestion domain

## Notes

- `native-mount-cp`: a full read of one 256 MiB file through the OS NFS client
- `native-mount-cp`: mount options carry `rsize=131072` and no `readahead=` (`consume.rs:588-599`), so this is the depth-1 path finding #1 describes
- `native-mount-cp`: the default path: plain iroh QUIC over UDP, no WebRTC wrapper
- `native-mount-cp-webrtc`: a full read of one 256 MiB file through the OS NFS client
- `native-mount-cp-webrtc`: mount options carry `rsize=131072` and no `readahead=` (`consume.rs:588-599`), so this is the depth-1 path finding #1 describes
- `native-mount-cp-webrtc`: forced onto the WebRTC lane with `--transport webrtc`. Its control is `native-mount-cp`: same NFS client, same `rsize`, same producer code. The pair also differs in discovery — WebRTC signalling needs a relay, so this leg uses the default ladder rather than a loopback swarm — but both move data over a host-local path
