# agent-share performance baseline

Generated by `cargo task bench --tag depth-sweep`. Do not hand-edit — rerun it.

## Provenance

- commit: `e44acafc` (dirty working tree — numbers are not reproducible from this SHA)
- machine: Mac17,2 (10 cores)
- os: macOS 27.0
- rustc: rustc 1.95.0 (59807616e 2026-04-14)
- chrome: 149.0.7827.54
- wasm: 7381312 bytes
- corpus: 1024 MiB · bench window: 8 s · repeats: 3

## Results

| cell | transport | direction | depth | RTT (ms) | MiB/s | connect (ms) | cores busy | user/sys (s) | spread |
|---|---|---|---:|---:|---:|---:|---:|---:|---:|
| `native-synth-quic` | quic | native->native | 1 | 0.07 | 115.04 | 13.9 | 2.48 | 1.7 / 7.2 | 1% |
| `native-synth-quic-d2` | quic | native->native | 2 | 0.55 | 117.81 | 13.9 | 2.71 | 1.9 / 8.7 | 1% |
| `native-synth-quic-d4` | quic | native->native | 4 | 0.62 | 115.31 | 12.2 | 2.82 | 2.0 / 9.2 | 1% |
| `native-synth-quic-d8` | quic | native->native | 8 | 1.06 | 69.66 | 12.5 | 2.51 | 2.2 / 8.2 | 33% |
| `native-synth-webrtc` | webrtc | native->native | 1 | 3.63 | 18.31 | 1096.2 | 1.79 | 2.0 / 5.5 | 19% |
| `native-synth-webrtc-d2` | webrtc | native->native | 2 | 13.48 | 8.11 | 1079.6 | 1.91 | 1.8 / 6.5 | 16% |
| `native-synth-webrtc-d4` | webrtc | native->native | 4 | 18.97 | 7.69 | 1074.6 | 1.91 | 1.9 / 6.6 | 44% |
| `native-synth-webrtc-d8` | webrtc | native->native | 8 | 18.47 | 7.74 | 1121.2 | 1.93 | 1.8 / 6.9 | 19% |

`cores busy` is CPU-seconds per wall-second across producer and consumer: above 1.0 the cell is compute-bound, not waiting on the network. `user/sys` is the consumer's split — ciphers and copies land in `user`, syscalls and context switches in `sys`.

Mesh state per row:

- `native-synth-quic`: none (bench does not join a share mesh)
- `native-synth-quic-d2`: none (bench does not join a share mesh)
- `native-synth-quic-d4`: none (bench does not join a share mesh)
- `native-synth-quic-d8`: none (bench does not join a share mesh)
- `native-synth-webrtc`: none (bench does not join a share mesh)
- `native-synth-webrtc-d2`: none (bench does not join a share mesh)
- `native-synth-webrtc-d4`: none (bench does not join a share mesh)
- `native-synth-webrtc-d8`: none (bench does not join a share mesh)

## Notes

- `native-synth-quic`: single-stream serial fill (`mount/bench.rs:493`) — a latency probe and a floor, not mount throughput
- `native-synth-quic`: the control leg — plain iroh QUIC over UDP, nothing forced and nothing cleared
- `native-synth-quic-d2`: single-stream serial fill (`mount/bench.rs:493`) — a latency probe and a floor, not mount throughput
- `native-synth-quic-d2`: at depth > 1 the RTT column measures queueing, not path latency: the echo probe queues behind the in-flight fills
- `native-synth-quic-d2`: the control leg — plain iroh QUIC over UDP, nothing forced and nothing cleared
- `native-synth-quic-d4`: single-stream serial fill (`mount/bench.rs:493`) — a latency probe and a floor, not mount throughput
- `native-synth-quic-d4`: at depth > 1 the RTT column measures queueing, not path latency: the echo probe queues behind the in-flight fills
- `native-synth-quic-d4`: the control leg — plain iroh QUIC over UDP, nothing forced and nothing cleared
- `native-synth-quic-d8`: single-stream serial fill (`mount/bench.rs:493`) — a latency probe and a floor, not mount throughput
- `native-synth-quic-d8`: at depth > 1 the RTT column measures queueing, not path latency: the echo probe queues behind the in-flight fills
- `native-synth-quic-d8`: the control leg — plain iroh QUIC over UDP, nothing forced and nothing cleared
- `native-synth-webrtc`: single-stream serial fill (`mount/bench.rs:493`) — a latency probe and a floor, not mount throughput
- `native-synth-webrtc`: QUIC inside a WebRTC data channel: bytes are encrypted twice, and `host/sender.rs:52` undoes GSO batching into one data-channel message per QUIC datagram
- `native-synth-webrtc-d2`: single-stream serial fill (`mount/bench.rs:493`) — a latency probe and a floor, not mount throughput
- `native-synth-webrtc-d2`: at depth > 1 the RTT column measures queueing, not path latency: the echo probe queues behind the in-flight fills
- `native-synth-webrtc-d2`: QUIC inside a WebRTC data channel: bytes are encrypted twice, and `host/sender.rs:52` undoes GSO batching into one data-channel message per QUIC datagram
- `native-synth-webrtc-d4`: single-stream serial fill (`mount/bench.rs:493`) — a latency probe and a floor, not mount throughput
- `native-synth-webrtc-d4`: at depth > 1 the RTT column measures queueing, not path latency: the echo probe queues behind the in-flight fills
- `native-synth-webrtc-d4`: QUIC inside a WebRTC data channel: bytes are encrypted twice, and `host/sender.rs:52` undoes GSO batching into one data-channel message per QUIC datagram
- `native-synth-webrtc-d8`: single-stream serial fill (`mount/bench.rs:493`) — a latency probe and a floor, not mount throughput
- `native-synth-webrtc-d8`: at depth > 1 the RTT column measures queueing, not path latency: the echo probe queues behind the in-flight fills
- `native-synth-webrtc-d8`: QUIC inside a WebRTC data channel: bytes are encrypted twice, and `host/sender.rs:52` undoes GSO batching into one data-channel message per QUIC datagram
