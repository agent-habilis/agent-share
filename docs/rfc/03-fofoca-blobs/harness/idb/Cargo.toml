[package]
name = "s06-idb"
version = "0.0.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
wasm-bindgen-futures = "0.4"
js-sys = "0.3"
anyhow = "1"
fofoca-blobs = { path = "../../../../../crates/fofoca-blobs" }

# Excluded from the repo workspace: a spike, not a product.
[workspace]
