# Bun does not walk up for a bunfig, so `cd packages/moonspace-theme && bun test`
# would otherwise run with no DOM registered. The root run picks up the root bunfig.
[test]
preload = ["../../test-setup.ts"]
