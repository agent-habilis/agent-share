{
  "name": "agent-share-web",
  "private": true,
  "type": "module",
  "workspaces": ["vendor/*"],
  "scripts": {
    "dev": "bun --hot scripts/dev.ts",
    "build": "bun scripts/build.ts",
    "preview": "bun scripts/preview.ts",
    "typecheck": "bun scripts/gen-wasm-path.ts && tsc --noEmit -p tsconfig.json && tsc --noEmit -p vendor/visage-dom && tsc --noEmit -p vendor/visage-style && tsc --noEmit -p vendor/visage-router && tsc --noEmit -p vendor/moonspace && tsc --noEmit -p vendor/moonspace-theme && tsc --noEmit -p vendor/moonspace-dom",
    "test": "bun scripts/gen-wasm-path.ts && bun test"
  },
  "dependencies": {
    "client-zip": "^2.4.5",
    "moonspace": "workspace:*",
    "moonspace-dom": "workspace:*",
    "moonspace-theme": "workspace:*",
    "visage-dom": "workspace:*",
    "visage-router": "workspace:*",
    "visage-style": "workspace:*"
  },
  "devDependencies": {
    "@happy-dom/global-registrator": "^20.11.1",
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
