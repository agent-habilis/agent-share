{
  "name": "agent-share-app",
  "version": "0.0.0",
  "description": "The browser app: entry point, routes, service worker and the lab.",
  "type": "module",
  "private": true,
  "dependencies": {
    "agent-share-core": "workspace:*",
    "agent-share-ui": "workspace:*",
    "agent-share-wasm": "workspace:*",
    "moonspace-dom": "workspace:*",
    "visage-dom": "workspace:*",
    "visage-router": "workspace:*"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  }
}
