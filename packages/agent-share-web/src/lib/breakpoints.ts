/**
 * A phone in portrait, where the top bar stacks its slots instead of lining
 * them up. `NARROW` is a key for `css()`; the px value is for a rule that has
 * to name the other side of the line.
 */
export const NARROW_PX = 640
export const NARROW = `@media (max-width: ${NARROW_PX}px)`
